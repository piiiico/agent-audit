name = "agent-audit-mcp"
main = "worker/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = ""

# Custom domain can be added later
# routes = [{ pattern = "agent-audit.piiiico.dev", custom_domain = true }]
